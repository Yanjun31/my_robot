This is the package for the Lab 4 of EECS 473.

Use the following line to view the robot description in RVIZ with the joint_state_publisher GUI:
roslaunch my_robot display.launch

Use the following line to view the robot description in RVIZ without the joint_state_publisher GUI:
roslaunch my_robot display.launch gui:=false

